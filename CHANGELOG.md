## 0.0.2

* Fix deprecated `Color.withOpacity` usage (replaced with `Color.withValues`).
* Update metadata URLs in `pubspec.yaml` (homepage, repository, issue_tracker).

## 0.0.1

* Initial release of `cmx_capsule_nav`.
* Custom animated floating capsule-style navigation bar.
* Support for multiple items with morphing animations.
* Highly customizable theme and styling.
